{
  "ConnectionStrings": {
    "FootballMeetingsConnectionString": "Server=(localdb)\\mssqllocaldb;Database=EfFootballMeetingsDb;Trusted_Connection=True;"
  },
  "Authentication": {
    "JwtKey": "PRIVATE_KEY_DONT_SHARE",
    "JwtExpireDays": 7,
    "JwtIssuer": "http://football-meetings-api.com",
    "JwtAudience": "http://football-meetings-api.com"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedOrigins": "http://127.0.0.1:5173",
  "Sieve": {
    "CaseSensitive": true, //boolean: should property names be case-sensitive? Defaults to false
    "DefaultPageSize": 10, //"int number: optional number to fallback to when no page argument is given. Set <=0 to disable paging if no pageSize is specified (default).",
    "MaxPageSize": 20, //"int number: maximum allowed page size. Set <=0 to make infinite (default)",
    "ThrowExceptions": false, //"boolean: should Sieve throw exceptions instead of silently failing? Defaults to false",
    "IgnoreNullsOnNotEqual": true, //"boolean: ignore null values when filtering using is not equal operator? Defaults to true",
    "DisableNullableTypeExpressionForSorting": false //"boolean: disable the creation of nullable type expression for sorting. Some databases do not handle it (yet). Defaults to false"
  }
}
